#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include "myheader.h"

int main (int argc, char *argv[]) {
    // myrpog1(argc, argv);
    // myprog2(argc, argv);
    // myprog3(argc, argv);
    myprog4(argc, argv);
    return 0;
}